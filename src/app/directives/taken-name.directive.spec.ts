import { XssProtectionDirective } from './xss-protection.directive';

describe('XssProtectionDirective', () => {
  it('should create an instance', () => {
    const directive = new XssProtectionDirective();
    expect(directive).toBeTruthy();
  });
});
